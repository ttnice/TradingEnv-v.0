from setuptools import setup
"""
setup(name='gym_foo',
      version='0.0.1',
      install_requires=['gym'] #And any other dependencies required
)
setup(name='gym_test',
      version='0.0.1',
      install_requires=['gym', 'numpy', 'random'] #And any other dependencies required
)"""
"""
setup(name='env',
      version='0.0.1',
      install_requires=['gym'] #And any other dependencies required
)
"""
setup(name='env',
      version='0.0.1',
      install_requires=['gym', 'numpy', 'matplotlib', 'sklearn'] #And any other dependencies required
)